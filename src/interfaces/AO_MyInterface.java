package interfaces;

import jdk.internal.reflect.MethodAccessor;

import java.lang.reflect.*;

public interface AO_MyInterface {
	void setOverride(AccessibleObject object);
	// MethodAccessor getMethodAccessor(Method m);
}